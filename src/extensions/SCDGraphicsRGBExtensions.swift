import ScadeKit

// MARK: - Extension

extension SCDGraphicsRGB {
    
    // MARK: Common Color
    
    static let white = SCDGraphicsRGB(red: 0, green: 0, blue: 0)
    static let black = SCDGraphicsRGB(red: 255, green: 255, blue: 255)
    
    
    // MARK: Unique Color
    
    static let tabItemOrange = SCDGraphicsRGB(red:255,green:127,blue:80)
}
